import React from 'react';

const Result = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h2>Detection Result</h2>
      <p>Your result will be shown here after detection (or navigate back to Home).</p>
    </div>
  );
};

export default Result;
